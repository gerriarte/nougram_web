{
    "buildCommand": "npm run build",
    "framework": "react-router",
    "rewrites": [
        {
            "source": "/auth",
            "destination": "/api/auth"
        },
        {
            "source": "/callback",
            "destination": "/api/callback"
        }
    ]
}